# nixpacks.toml - Fixed for Railway
[phases.setup]
nixPkgs = ["ffmpeg", "python310"]

[phases.install]
cmds = [
  "python3 -m ensurepip --upgrade",
  "python3 -m pip install yt-dlp",
  "npm install"
]

[start]
cmd = "node server.js"
